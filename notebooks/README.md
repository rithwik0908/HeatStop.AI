No notebook is required for the MVP because preprocessing is handled by `scripts/build_demo_data.py`.

If you want an exploratory notebook later, keep it tied to the real CSV and GeoJSON files in `data/processed/`.
