`data/processed/` is generated by `python scripts/build_demo_data.py`.

Expected outputs:

- `stops_scored.csv`
- `stops_scored.geojson`
- `build_meta.json`

If these files are missing, the API and UI show a clean empty state instead of fake demo data.
